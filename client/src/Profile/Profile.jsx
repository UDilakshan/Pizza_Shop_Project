import React from "react";
import { ProLeftSection, ProRightSection } from "../components";

const Profile = () => {
    return (
        <div className="w-screen h-screen flex items-center bg-primary">
            <ProLeftSection />
            <ProRightSection />
        </div>
    );
};

export default Profile;
